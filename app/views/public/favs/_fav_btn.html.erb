<% if post.favorited_by?(current_user) %>
  <p>
    <%= link_to post_favs_path(post), method: :delete, remote: true do %>
      ♥<%= post.favs.count %>
    <% end %>
  </p>
  <% else %>
  <p>
    <%= link_to post_favs_path(post), method: :post, remote: true do %>
      ♡<%= post.favs.count %>
    <% end %>
  </p>
<% end %>