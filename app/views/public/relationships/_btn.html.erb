<% if current_user.following?(user) %>
  <%= link_to "Unfollow", user_relationships_path(user.id), method: :delete, class: "btn unfollow_btn" %>
<% else %>
  <%= link_to "Follow", user_relationships_path(user.id), method: :post, class: "btn follow_btn" %>
<% end %>