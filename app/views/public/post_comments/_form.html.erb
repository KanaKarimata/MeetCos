<%= form_with model: [post, comment_new], local: false do |f| %>
  <%= f.text_field :comment, rows: '5', placeholder: "Any comments?", id: "comment-textarea-#{post.id}" %>
  <%= f.submit "Send" %>
<% end %>